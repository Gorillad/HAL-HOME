You May Like — optional fallback preview images
===============================================

Upload lifestyle photos in the editor for each carousel slot (500 × 750 px).

Optional: drop fallback preview images here named by item number (1001.png, etc.)
if you want placeholders before images are uploaded in the editor.

On the live site, the section title, product name, and price come from the
You May Like dashboard attribute using each item number.
